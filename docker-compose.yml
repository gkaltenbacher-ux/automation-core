services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
      - ./blocks:/app/blocks
    env_file:
      - .env
    restart: unless-stopped
